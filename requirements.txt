# Core
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
psycopg[binary]==3.2.2
psycopg-pool==3.2.2

# AI (OpenAI SDK with Groq backend - OpenAI-compatible API)
openai>=1.0.0

# HTTP Client
httpx==0.26.0

# Utils
python-dotenv==1.0.1
python-multipart==0.0.9
pyyaml==6.0.1
Pillow>=10.0.0
bcrypt>=4.0.0  # admin_users password hashing (app/booking/admin_router.py)

# Timezone
pytz==2024.1
tzlocal==5.2

# Email
resend>=0.8.0
boto3>=1.34
cryptography>=42.0  # SNS message signature verification for the SES webhook

# Booking Platform - Payment
mercadopago>=2.2.0
transbank-sdk>=6.0.0

# Web Push notifications (PWA)
pywebpush>=1.9.4,<2.0.0

# Audio conversion (webm→ogg for WhatsApp voice messages)
imageio-ffmpeg>=0.4.9

# Google Sheets bidirectional sync (Flujo de Caja ledger)
gspread>=6.0.0
google-auth>=2.28.0








